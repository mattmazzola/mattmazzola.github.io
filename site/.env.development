# Development environment variables
# These point to fixture data served by the Vite dev server (public/dev/).
# The same env var names are used in production but point to Azure Blob Storage URLs.

LINKS_JSON_BLOB_URL=http://localhost:3999/links.json
PROJECTS_JSON_BLOB_URL=http://localhost:3999/projects.json
