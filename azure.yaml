# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: mattmazzolagithubio

metadata:
  template: azd-init@1.20.3

services:
  site:
    project: site
    host: containerapp
    language: docker
    docker:
      path: Dockerfile

resources:
  site:
    type: host.containerapp
    port: 8080
